import { Container } from "components/styled";
import styled from "styled-components";
import Image from "next/image";

export const StyledCanvas = styled(Image)``;

export const CanvasContainer = styled(Container)`
  padding: 1.5rem;
`;
