import { Button } from "../styled";
import { StyledNavbar } from "./styles";

const NavBar = () => {
  return (
    <StyledNavbar>
      <Button>Log in</Button>
      <Button>Log in</Button>
    </StyledNavbar>
  );
};

export default NavBar;
